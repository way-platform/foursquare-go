# GoReleaser configuration for the foursquare CLI.
#
# The repository root package is the importable `foursquare` library, which has no
# main function. The CLI entry point lives in ./cmd/foursquare, so the build must
# point there explicitly; otherwise GoReleaser defaults to "." and fails with
# "does not contain a main function".
version: 2

builds:
  - id: foursquare
    binary: foursquare
    main: ./cmd/foursquare
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt
